{
  "name": "farmaxia",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "dev": "pnpm --parallel --if-present --filter @farmaxia/api --filter @farmaxia/web run dev"
  },
  "devDependencies": { "typescript": "7.0.2" }
}
